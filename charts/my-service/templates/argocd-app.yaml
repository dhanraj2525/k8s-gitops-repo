apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: my-service-prod
  namespace: argocd
  labels:
    app: my-service
    env: production
spec:
  project: default
  source:
    repoURL: https://bitbucket.org/my-org/k8s-gitops-repo.git
    targetRevision: HEAD
    path: charts/my-service
    helm:
      valueFiles:
        - values.yaml
        - values-prod.yaml     # Jenkins updates image.tag here
  destination:
    server: https://kubernetes.default.svc
    namespace: production
  syncPolicy:
    automated:
      prune: true              # Remove resources deleted from Git
      selfHeal: true           # Auto-fix any manual cluster changes
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        maxDuration: 2m
        factor: 2